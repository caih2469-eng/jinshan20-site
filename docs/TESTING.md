# 测试与检查

## 2026-07-24 审计结果

## 阶段 1 自动测试

运行：

```powershell
node --test
```

结果：2 个测试通过，0 个失败。覆盖：

- 旧数据库迁移和两个赛道初始化。
- 管理员沿用现有登录流程登录。
- 登录和资料接口不返回密码。
- 管理员添加、编辑、禁用/启用用户。
- 普通用户只能查看自己的资料。
- 普通用户访问用户列表或编辑接口返回 403。
- Excel `.xlsx` 名单导入。
- 禁用账号的已有令牌失效，且不能重新登录。

## 阶段 2 自动测试

运行完整测试后结果为：4 个测试通过，0 个失败。

阶段 2 覆盖：

- 默认最大队伍数量为 50，并持久化在数据库。
- 迁移重复执行保持不变。
- 增加和减少队伍名额。
- 队伍名额已满时禁止创建。
- 创建队伍、修改队伍名称和人数限制。
- 邀请码大小写兼容加入。
- 一个学生只能加入一个队伍。
- 自律健康赛道禁止加入队伍。
- 满员队伍禁止继续加入。
- 人数限制不得低于当前成员数。
- 管理员查看及移除成员。
- 有成员队伍禁止删除，空队伍允许解散。
- 最大队伍数不得低于现有队伍数量。

## 阶段 3、4 自动测试

运行完整测试后结果为：6 个测试通过，0 个失败。新增覆盖：

- 任务和提交迁移可重复执行。
- 活动/赛道开关、任务状态和服务端时间校验。
- 队伍唯一提交、个人三餐提交和历史记录。
- 草稿、最终提交、重复提交阻止和并发版本冲突。
- 图片对象写入存储目录，数据库仅保存 URL。

## 阶段 5 自动测试

运行完整测试后结果为：8 个测试通过，0 个失败。新增覆盖：

- 阶段 5 迁移可重复执行。
- 公开四校区最终提交自动生成唯一帖子。
- 自律赛道提交不生成帖子，学生自由发帖被服务端拒绝。
- 最新/月度读取、分页字段、浏览计数和点赞切换。
- 管理员隐藏、恢复、删除及前台可见性。

## 阶段 7 自动测试

完整测试结果为：12 个测试通过，0 个失败。新增覆盖：

- 阶段 7 迁移和排名排除字段。
- 点赞榜、浏览榜及 70/30 归一化综合热度顺序。
- 月度队伍公开、点赞、浏览统计。
- 月榜冻结幂等保护和冻结快照读取。
- 导出权限、中文 Excel 内容和冻结文件名。

## 阶段 8 自动测试

完整测试结果为：13 个测试通过，0 个失败。新增覆盖：

- 六项后台看板统计和普通用户越权阻止。
- 广场帖子排除排名后即时从榜单消失。
- 六类 Excel 导出均可被 ExcelJS 重新读取。
- 普通用户访问全部导出接口返回 `403`。
- 18 位学号保持字符串并使用 Excel 文本格式。
- 删除提交级联清理帖子、点赞和浏览记录。

## 阶段 9 自动测试

完整测试结果为：15 个测试通过，0 个失败。新增覆盖：

- 阶段 9 迁移可重复执行。
- 个人和队伍材料提交、文件类型/数量/总结规则。
- 队伍成员共享下载权限以及非成员越权阻止。
- 未登录、其他学生、本人和管理员的文件访问权限。
- 管理员退回原因、队友版本化重新提交。
- 未提交名单 Excel 和长学号文本格式。

## 定时任务与统一编队测试

完整测试结果为：17 个测试通过，0 个失败。新增覆盖：

- 活动第几天/指定星期任务只在匹配当天出现。
- 当天 occurrence、最多 3 张、不补交和版本唯一性。
- 发布广场必须填写独立作品文案。
- 学生自助加入关闭。
- Excel 一次导入四人队伍。
- 管理员踢出和按学号加入成员。

## 阶段 6 自动测试

运行完整测试后结果为：10 个测试通过，0 个失败。新增并发覆盖：

- 多窗口同时对同一帖子点赞只生成一条记录。
- 同时点赞多篇帖子时最多 5 个成功，第 6 个返回 `429`。
- 重复请求保持幂等，取消点赞恢复额度并可用于其他帖子。
- 同用户并发进入详情仅一次增加浏览量。
- 管理员访问不增加浏览量。
- 保存后的点赞和浏览组合键无重复。

### 已运行

| 检查 | 结果 |
| --- | --- |
| `node --check server.js` | 通过 |
| `node --check public/app.js` | 通过 |
| 解析 `data/db.json` | 通过 |
| 启动 `node server.js` | 通过 |
| `GET /` | HTTP 200 |
| 管理员 `POST /api/login` | 通过 |
| 带令牌 `GET /api/me` | 通过，角色为 `admin`，配置含 3 个时段 |
| Git 工作区与远端 | `main` 跟踪 `origin/main` |
| GitHub Actions | 0 个工作流 |
| GitHub Pages | 未配置 |

### 无法运行

| 检查 | 原因 |
| --- | --- |
| lint | 有 `package.json`，但尚未引入 ESLint |
| 类型检查 | 项目是原生 JavaScript，没有 TypeScript/JSDoc 类型检查配置 |
| 构建 | 静态资源直接提供，没有构建工具或 build 脚本 |
| 自动化测试 | 已使用 Node.js 内置测试框架 |
| Cloudflare 构建/预览 | 没有 Wrangler 或任何 Cloudflare 配置 |

“无法运行”表示对应工具链不存在，不表示这些检查通过。

## 当前手动验证命令

```powershell
node --check server.js
node --check public/app.js
node -e "JSON.parse(require('fs').readFileSync('data/db.json','utf8'))"
node server.js
```

启动后至少验证：

1. `GET /` 返回 200。
2. 错误密码返回 401。
3. 管理员登录后能读取 `/api/me` 和 `/api/admin/dashboard`。
4. 学生不能调用管理员接口。
5. 非当前日期和时段外打卡被拒绝。
6. 同一时段更新只保留一条逻辑记录。
7. 管理员通过/驳回状态能正确显示。

## 建议的自动化测试范围

### API

- 登录成功、失败、限流和会话过期。
- 角色权限矩阵。
- 活动起止日期和时段边界：前一秒、起始、结束、后一秒。
- 重复提交、并发提交、文件失败和数据库回滚。
- 审核状态转换和审计日志。

### 安全

- 阶段 10 自动检查签名令牌、管理员接口隔离、跨用户写入拒绝、上传签名验证、安全响应头和登录限流。
- 最终截图后台按校区统计并以 50 条分页读取。
- 本地轻量并发烟雾测试：700 个并发静态读取全部返回 200，耗时约 476ms。该结果不替代 Cloudflare D1/R2 预览环境的阶梯压测。

- 令牌伪造、越权读取、路径遍历、上传 MIME 欺骗。
- XSS、恶意配置值、超大请求和暴力登录。
- 确认所有响应不包含密码或密码哈希。
- 确认上传对象未登录无法读取。

### 前端

- 宣传页、登录、学生三时段、管理员表格的移动端布局。
- 配置变更后所有页面文案和时段一致。
- `pending`、`approved`、`rejected` 三种状态显示。

### 部署

- GitHub Actions 运行 lint、类型检查、测试和构建。
- Cloudflare 预览环境验证 D1 migrations、R2 bindings 和 secrets。
- 生产部署前执行备份、回滚和隐私验收。

## 2026-07-30 Draft PR 阻塞项验证

### 历史广场缩略图

只在测试 D1 `jinshan20-test` 与测试 R2 `jinshan20-test` 执行：

- 全量广场媒体：2 张；
- 执行前缺失 `thumb`：1 张；
- 成功补全：1 张；
- 失败：0；
- 新增 R2 对象：1；
- 新增 D1 `image_variants` 记录：1；
- 执行后缺失：0；
- 重复执行新增对象与记录：均为 0。

缺失媒体：

- 媒体 ID：`c4d96fa5-fd0c-42be-9e4d-000d75ed117e`
- 帖子 ID：`f9d741dd-3a31-494a-9865-ee66d79e970a`
- 原图：465,730B JPEG
- 补全结果：27,742B WebP、360×203、质量参数 84

脚本默认只做 dry-run；应用时使用：

```powershell
npm run backfill:plaza-thumbs -- --apply
```

脚本硬性拒绝非 `test` 环境、名称不以 `-test` 结尾的 D1/R2 资源，以及非
`pages-test/wrangler.jsonc` 配置。正式资源未读取、未修改。

### 中文编码

- `npm run check:text`：严格 UTF-8 解码及乱码替换串扫描通过，0 项违规。
- 7 个旧 UTF-16LE 报告已无损转换为 UTF-8。
- 测试 D1 中早期问号替换数据已改为明确测试文案。
- 正式 D1 未读取、未修改。

### 自动化结果

| 命令 | 结果 |
| --- | --- |
| `npm run check` | 通过 |
| `npm run check:text` | 通过 |
| `npm test` | 59/59 通过 |

### 真机状态

Android 微信、Android QQ、Android Chrome、iPhone 微信、iPhone QQ 和
iPhone Safari 均没有真实设备记录，统一判定：**未通过**。详细执行表见
`docs/MANUAL_DEVICE_TEST_CHECKLIST.md`。

## 2026-08-12 严格 Linux Chrome 验收

- 功能代码基线：`697c984e86f32667cb80db116edba49d06d8f842`；后续仅补充本验收记录。
- 静态与功能回归：194/194 通过；UTF-8 文本审计 0 项违规。
- 本机 Ubuntu、Google Chrome 151、Fast-4G、20 次冷启动：入口 p95 972.1ms，登录到首页 p95 429.7ms，首页到活动广场 p95 568.5ms，活动广场到详情 p95 22.6ms，严格门禁通过。
- GitHub Ubuntu、Google Chrome、Fast-4G、20 次冷启动：入口 p95 375.0ms，登录到首页 p95 775.9ms，首页到活动广场 p95 280.1ms，活动广场到详情 p95 31.9ms，严格门禁通过（Run 31522536737）。
- 本机冷缓存广场首图 329.3ms；返回/最新/热门/搜索、公开作品上传双变体及父子媒体关联均通过。
- 图片质量策略未降低：列表继续使用 960 档，详情继续使用 2048 档；个人打卡图片数量按管理员 `memberImageLimit` 设置执行，并保留旧任务的 `imageLimit` 回退。
